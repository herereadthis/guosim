# Guosim

Shell scripts

## flac_to_mp3.sh

Converts a directory of `.flac` files to best possible `.mp3` format

### Prerequisites

```shell
# install ffmpeg
brew install ffmpeg
```

### Usage

```shell
./flac_to_mp3.sh /path/to/directory
```